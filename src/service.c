#include <stdio.h>
#include "nest_list.h"


int 
main()
{
    //if(conf_init() != 0) {
    //    printf("configure init error\n");
    //    exit(-1);
    //}
    //conf_val mysql_host;
    //mysql_host = conf_get_option_val(CONF_MYSQL, CONF_MYSQL_HOST);
    //printf("%s\n", mysql_host);
    //conf_free();
    return 0;
}
